{
  "name": "gng-browser",
  "version": "0.1.2",
  "private": true,
  "description": "Custom Electron browser with a built-in Naver automation agent, attachable over CDP",
  "license": "MIT",
  "author": "ganggyunggyu",
  "homepage": "https://gng-browser.vercel.app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ganggyunggyu/gng-browser.git"
  },
  "bugs": {
    "url": "https://github.com/ganggyunggyu/gng-browser/issues"
  },
  "engines": {
    "node": ">=22"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && electron dist/main.js",
    "dist": "npm run build && electron-builder --publish never",
    "typecheck": "tsc --noEmit",
    "install:app": "npm run dist && rm -rf \"/Applications/GNG Browser.app\" && cp -R \"out/mac-arm64/GNG Browser.app\" /Applications/ && echo \"설치 완료: /Applications/GNG Browser.app\"",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "check": "npm run typecheck && npm run test"
  },
  "build": {
    "appId": "kr.gng.browser",
    "productName": "GNG Browser",
    "directories": {
      "output": "out",
      "buildResources": "build-resources"
    },
    "files": [
      "dist/**/*"
    ],
    "afterPack": "scripts/adhoc-sign.mjs",
    "mac": {
      "icon": "build-resources/icon.icns",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "category": "public.app-category.productivity",
      "identity": null,
      "artifactName": "GNG-Browser-mac-${arch}.${ext}"
    },
    "win": {
      "icon": "build-resources/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "GNG-Browser-Setup-win-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^43.1.1",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "playwright-core": "^1.62.1"
  }
}
